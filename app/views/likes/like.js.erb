$('.stuff_item[data-stuff-id=<%=params[:stuff_id]%>]').find('a.like').each(function(i, obj) {
  obj.classList.add('yellow');
});

$('.stuff_item[data-stuff-id=<%=params[:stuff_id]%>]').find('.likes_count').get(0).innerHTML = "<%= @likes.count %>"
